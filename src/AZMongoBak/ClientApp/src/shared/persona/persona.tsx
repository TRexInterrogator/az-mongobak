import { CSSProperties } from "react";

interface IPersonaProps {
    img?: string;
    name: string;
    style?: CSSProperties;
}

export const Persona = (props: IPersonaProps) => {

    // TODO

    return (
        <div>
            
        </div>
    );
}